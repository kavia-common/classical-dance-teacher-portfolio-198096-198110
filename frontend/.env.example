# When deploying, set one of these to point to the backend.
# If left empty, apiFetch defaults to same-origin and your reverse-proxy must route /api/* to the backend.

# Example: http://localhost:3010
REACT_APP_BACKEND_URL=

# Alternative keys supported by apiFetch:
REACT_APP_API_BASE=
VITE_API_BASE=
