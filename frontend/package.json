{
  "name": "classical-dance-teacher-profile",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port ${PORT:-3000}",
    "start": "vite --host 0.0.0.0 --port ${PORT:-3000}",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port ${PORT:-3000}"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.1.0",
    "dotenv": "^17.2.3",
    "vite": "^4.4.0"
  }
}
