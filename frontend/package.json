{
  "name": "classical-dance-teacher-profile",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "vite build && vite preview --host 0.0.0.0 --port 3000",
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3000"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "vite": "^5.4.10",
    "@vitejs/plugin-react": "^4.3.4",
    "rollup": "^4.24.0",
    "esbuild": "^0.21.5"
  }
}
