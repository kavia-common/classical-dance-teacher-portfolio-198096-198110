/**
 * PUBLIC_INTERFACE
 * Page-level exports (do not import in App routing to avoid duplicate admin pages).
 * Admin routing uses src/admin/*.jsx. This export remains for potential external usage.
 */
export { default as AdminGallery } from './AdminGallery';
